class Actor:
    def __init__(self, actor, actor_type, actor_id, actor_name, actor_transform):
        self.actor = actor
        self.actor_type = actor_type
        self.actor_id = actor_id
        self.actor_name = actor_name
        self.actor_transform = actor_transform
    pass
